# CLAUDE.md

Guidance for Claude Code working in this repository. Read this before making changes, and keep it updated as the infrastructure and our working agreements evolve.

## What this repo is

Infrastructure-as-Code for zanyhunter's homelab. The goal is disaster recovery: everything needed to stand up equivalent infrastructure on fresh hardware should be reconstructable from this repo plus the age private key (kept in KeePass, see Secrets below). The Kubernetes cluster is still dev-only, but this is no longer strictly true in practice: Immich (see below) is a real photo-library app, reachable publicly, that will hold real photos once actually used day-to-day — the "cluster is disposable, nothing real lives there" framing behind the full-autonomy guardrail further down is worth re-checking with the user as Immich sees real use, rather than assumed to still hold unconditionally. The Cloudflare API token also now manages real public DNS/Tunnel state for a real, internet-facing domain, not just DNS-01 challenges — treat that boundary seriously even though the cluster itself is still disposable.

The Tofu config is also meant to be **reusable across multiple cluster environments (dev today, a future prod) for years to come** — and dev/prod will not share infrastructure (each gets its own NFS backend, for instance). Environment-specific values (Helm chart versions, the NFS storage server/share, etc.) belong in object-typed variables in each unit's `variables.tf` (see `cluster`, `chart_versions`, `nfs_storage`) with real values in that environment's `tofu/live/<env>/env.hcl` (see `docs/src/explanation/terragrunt-units.md`), not hardcoded in resource blocks — when adding a new add-on or resource with a version string or environment-specific setting, variablize it the same way rather than hardcoding it "for now."

## Physical & network topology

- **Compute**: 3 physical Proxmox nodes (`pve-node-0/1/2`) in a hyperconverged cluster (`homelab`) using Ceph for storage (`ceph-1` datastore).
- **Storage**: 1 NFS NAS for service data (Immich, etc.). Not yet under IaC management — provisioned/configured manually today. Revisit if it should be brought into Tofu or get its own IaC story.
- **Network**: Unifi-managed, one VLAN per cluster. Prod lives on VLAN 1601, subnet `192.168.160.0/27` (the range dev originally occupied, expanded from a `/28` once that filled up — see issue #3), gateway `192.168.160.1`; dev lives on VLAN 1602, subnet `192.168.160.32/27`, gateway `192.168.160.33` (see the `network` unit, `tofu/modules/network/main.tf`, and each environment's `env.hcl`). Every ingress hostname/OIDC redirect URI across every unit is driven by one per-environment `domain_name` value in `env.hcl` (dev: `dev.thepugh.family`; prod: the real `thepugh.family` apex — see #10). Local DNS resolution for that whole suffix is a Tofu-managed wildcard record (`unifi_dns_record.wildcard_ingress`, same `network` unit) pointed at a static `ingress_ip` (also `env.hcl`) that `core-addons` pins ingress-nginx's LoadBalancer Service to — documented in `docs/src/explanation/dns-and-hostnames.md`.
- **Real external domain**: `thepugh.family`, managed in Cloudflare. cert-manager uses a Cloudflare API token (DNS Zone Edit scope) for ACME DNS-01 challenges — this is the one credential in this repo with blast radius outside the homelab.
- **Docs site's real external domain**: `zanylights.com` (specifically `zanylights.com/homelab/`) — a separate, unrelated custom domain GitHub Pages serves this repo's mdBook site from (see Documentation section below). Not Cloudflare/Tofu-managed; configured directly in the repo's GitHub Pages settings.

## Kubernetes cluster

- Talos Linux, provisioned as Proxmox VMs via the `siderolabs/talos` and `bpg/proxmox` Tofu providers.
- 6 nodes per environment: prod is `k8s-ctrl-00/01/02` (control plane) + `k8s-node-00/01/02` (worker); dev's carry a `-dev-` segment so the two environments' hostnames never collide (`k8s-dev-ctrl-00/01/02` / `k8s-dev-node-00/01/02`) — one of each role per Proxmox node, both environments sharing the same 3 physical hosts. See each environment's `env.hcl`.
- **Node sizing**: `env.hcl`'s `node_resources` object (control-plane vs. worker cores/memory), not hardcoded per-node — control planes stay at 4 vCPU/4GB (nothing schedules there, Talos taints them by default); workers are 8 vCPU/16GB, resized up from the original 4 vCPU/4GB after that ceiling forced Loki's `chunksCache` off entirely and would have made Immich's ML service risky. Physical hosts are 3× i9-12900H (14 cores/20 threads, 64GB RAM each) — running dev and prod at these sizes simultaneously is a deliberate bet on that headroom (see issue #21), worth revisiting if either cluster's real workload grows. Resizing workers only (never control planes) means etcd quorum is never at risk — verified live via a real one-worker-at-a-time cordon/drain/reboot cycle, not just a Tofu apply.
- Cluster name `dev` or `prod`; each has its own Talos-managed floating VIP shared across its 3 control planes (not any single node's address), so the control plane stays reachable if one control-plane node is down — `192.168.160.2` for prod, `192.168.160.34` for dev (each is the first address after its environment's gateway, with the rest of that cluster's nodes numbered consecutively after it).
- **Terragrunt units**: the Tofu config is seven independently-applied units (`tofu/modules/{network,talos-cluster,core-addons,backup,keycloak-infra,keycloak-realm,observability}/`, wired by `tofu/live/{dev,prod}/*/terragrunt.hcl`), not one flat root module. A from-scratch stand-up or full teardown is one command — `terragrunt run --all --non-interactive -- apply -auto-approve` (or `destroy`) from `tofu/live/dev/` — verified live via a real destroy-then-recreate of the entire dev environment. Detailed docs: `docs/src/explanation/terragrunt-units.md`.
- Bootstrap add-ons (the `core-addons` unit): MetalLB, ingress-nginx, cert-manager, csi-driver-nfs/StorageClass (default, general file storage), ceph-csi-rbd/StorageClass (opt-in, database/block-semantics-sensitive workloads — see the Ceph storage bullet below), ArgoCD (HA, exposed at `argocd.<domain_name>`). Cluster-level *config* objects that used to be applied by Tofu `terraform_data`/`local-exec` provisioners — the MetalLB `IPAddressPool`/`L2Advertisement` (LB pool ranges, one per environment — `apps/cluster-addons/overlays/<env>/env-values.yaml`) and the `letsencrypt-prod`/`letsencrypt-staging` cert-manager `ClusterIssuer`s — are GitOps-managed now instead; see the app-of-apps entry below.
- **Ceph-backed storage**: a second StorageClass (`ceph-rbd-<cluster_name>`, opt-in, not default) for databases and other block-semantics-sensitive workloads, where NFS's fsync/locking semantics are a real correctness risk — general file storage stays on the default NFS StorageClass. ceph-csi (RBD, external-cluster mode) connects directly to the existing Proxmox Ceph cluster (`ceph-1` datastore, already backing Talos VM disks) — no Ceph daemons run inside Kubernetes, no new hardware. A dedicated pool (Tofu-managed, `proxmox_ceph_pool` in the `talos-cluster` unit) keeps Kubernetes' Ceph credential scoped away from the pool VM disks use. The CephX credential itself is manually bootstrapped (Ceph's own auth system, not Proxmox's — no Tofu resource for it) and stored in `tofu/secrets.enc.yaml` like every other provider credential. See `docs/src/explanation/ceph-backed-storage.md`.
- **App-of-apps**: an ArgoCD `ApplicationSet` (part of the `core-addons` unit, `var.gitops` for repo/revision) auto-discovers one `Application` per `apps/<app>/overlays/<cluster_name>` directory, so adding a new app is "add `apps/<app>/base/` plus an `overlays/<env>/` per existing environment, push" — no Tofu change, no manual `kubectl`/`argocd` command. Every `apps/<app>/` directory (seven today: `cluster-addons` plus the six real workloads below) is split into an environment-agnostic `base/` and a tiny per-environment `overlays/dev|prod/` (a small `ConfigMap` of real values + a `kustomization.yaml` `replacements:` block wiring them into `base/`'s placeholder tokens) — added specifically so `dev`/`prod` don't need two near-duplicate copies of each app's manifests for the handful of values (hostnames, StorageClass names) that actually differ. `apps/cluster-addons/` is the cluster-level-config directory mentioned above. Detailed docs: `docs/src/explanation/gitops-app-of-apps.md`.
- **Immich**: the first real app-of-apps workload (`apps/immich/`), reachable at `photos.<domain_name>` on the LAN/VPN (not currently public — see the Public ingress bullet below). Database on the Ceph-backed StorageClass (`tensorchord/vchord-postgres`, a purpose-built VectorChord image — not CloudNativePG, which is what immich-charts' own README recommends, to avoid a whole new operator/CRDs for one database), media library on the NFS-backed one. Auth via Keycloak's native OIDC support, same pattern as ArgoCD/Grafana. The chart itself is a pinned `helm template` render committed as static YAML (`apps/immich/immich.yaml`), not rendered live via Kustomize's `helmCharts:` — that combination has a real, currently-unresolved OCI-pulling bug (kubernetes-sigs/kustomize#4381). See `docs/src/explanation/immich.md`.
- **Five more self-hosted apps**: Actual Budget (`apps/actual/`), Paperless-ngx (`apps/paperless/`), Vaultwarden (`apps/vaultwarden/`), Homebox (`apps/inventory/`), and changedetection.io (`apps/changedetection/`) — all LAN/VPN-only like Immich, plain hand-rolled Kustomize manifests (no Helm charts) following Immich's storage-split pattern: SQLite/Postgres on the Ceph-backed StorageClass, bulk files (Paperless's document library) on the NFS-backed one. Four have native OIDC (Actual, Paperless-ngx, Vaultwarden, Homebox — Paperless-ngx additionally syncs `platform-admins` to Django superuser via its own group-claim support); changedetection.io has none, so it's the first *real* app to use the `oauth2-proxy` forward-auth template `sso-demo` exists to prove out. See `docs/src/explanation/self-hosted-apps.md`.
- **Public ingress**: a Tofu-managed Cloudflare Tunnel (`core-addons`, new `cloudflare` provider) — outbound-only, no port-forward — gated entirely on `public_ingress_enabled` (`env.hcl`; `false` for dev, `true` for prod). **Only prod is meant to be publicly exposed long-term**: dev's exposure was a temporary proof of the mechanism (real end-to-end logins, real Cloudflare edge connections) and has since been torn down — the user has an existing Immich elsewhere at `photos.thepugh.family` to migrate to a legacy domain before prod claims that hostname, and prod's own `domain_name` being the bare apex avoids the hostname-split complexity dev's exposure surfaced. Ingress rules, when enabled, are an explicit allowlist (`env.hcl`'s `public_apps`): each entry forwards its full hostname, while Keycloak gets exactly one path-scoped route (`^/realms/homelab/.*`, gated by `public_keycloak_realm`) so its admin console stays internal/VPN-only by construction — not a denylist rule that could miss a path. `cloudflared` is hand-rolled (no official chart), 2 replicas using Cloudflare Tunnel's native multi-connector support. **Public hostnames live one level under the real apex** (`public_apex_domain` + `public_hostname_suffix`), not under `domain_name` — Cloudflare's free Universal SSL only covers one subdomain level, found live as a real `ERR_SSL_VERSION_OR_CYPHER_MISMATCH` on dev's public hostname; prod doesn't need the suffix at all, since its `domain_name` is already the apex. `cloudflared` rewrites the Host header back to the internal hostname when forwarding. See `docs/src/explanation/public-ingress.md`.
- **Backup**: Velero + MinIO (the `backup` unit), also Tofu/Helm-managed rather than GitOps — MinIO is an S3-compatible gateway in front of the NFS share (its own data volume is just another PVC on the `nfs_storage`-backed StorageClass), and Velero backs up Kubernetes object definitions to it on a daily schedule (`var.backup`). CSI volume snapshots stay disabled, but File System Backup (`deployNodeAgent = true`, Velero's kopia integration) is enabled and opt-in per-pod via the `backup.velero.io/backup-volumes` annotation — added once Ceph-backed PVCs existed (#28) and needed real volume-data backup, not just object definitions (NFS-backed data still doesn't strictly need this, since restoring the PV object alone reconnects to data the NAS's own offsite backup already protects, but File System Backup covers both StorageClasses uniformly rather than needing a second, Ceph-only mechanism like CSI snapshots). Required moving the `velero` namespace to `privileged` PSA (node-agent's DaemonSet needs real host-level volume-path access) and an explicit `HOME=/tmp` env var on the main Velero container (kopia's repo path resolves off `$HOME`, which is unset under a non-root `runAsUser` with no matching `/etc/passwd` entry — found live as a real `mkdir /udmrepo: permission denied` on the first backup attempt). Detailed docs, including the restore procedure: `docs/src/explanation/backup-and-restore.md`.
- **SSO**: Keycloak + a hand-rolled Postgres `StatefulSet` (the `keycloak-infra` unit), reachable at `keycloak.k8s.thepugh.family`. The `homelab` realm and its OIDC clients (the `keycloak-realm` unit) are managed declaratively via the `keycloak` Terraform provider — same pattern as `unifi_network.this` — authenticating with the same Tofu-generated bootstrap admin credentials Keycloak itself is given. Splitting Keycloak's bootstrap from its realm config into two Terragrunt units (rather than one `tofu/sso.tf`) is what eliminated the old `-target=time_sleep.wait_for_keycloak` two-phase bootstrap for good — see `docs/src/explanation/terragrunt-units.md`. An oauth2-proxy + `whoami` forward-auth demo at `sso-demo.k8s.thepugh.family` is a permanent reference deployment, not a throwaway — it's the copy-paste template for real apps that need forward-auth. ArgoCD and Grafana both authenticate via Keycloak's native OIDC support too (local admin disabled on both), gated on membership in a `platform-admins` Keycloak group rather than any hardcoded person's identity — real accounts and group membership are managed by hand in Keycloak's admin console, not in Tofu. See `docs/src/explanation/sso-and-keycloak.md`.
- **Observability**: kube-prometheus-stack (Prometheus + Alertmanager + Grafana + node-exporter + kube-state-metrics) plus Loki + Grafana Alloy for logs (the `observability` unit), all Tofu/Helm-managed. It depends on `core-addons` like `backup`/`keycloak-infra` do, but — since Grafana's OIDC client secret (#32) is generated here and consumed by `keycloak-realm` — `keycloak-realm` now depends on `observability` too, so it's no longer an unordered sibling of `keycloak-infra`/`keycloak-realm` in the DAG; it applies before both. Grafana is reachable at `grafana.k8s.thepugh.family`; default cluster/node dashboards and most baseline alerts (node down, PVC nearly full, etc.) come from the chart's own defaults for free. Alertmanager routes to a Discord webhook (`discord_alert_webhook` in `tofu/secrets.enc.yaml`). cert-manager certificate-expiry and Velero backup-staleness alerts are added via the chart's own `additionalPrometheusRulesMap`/`additionalScrapeConfigs` values rather than separate `ServiceMonitor`/`PrometheusRule` custom resources in `core-addons`/`backup` — those would need this unit's Prometheus Operator CRDs to exist before `core-addons`/`backup` apply, which is backwards in the DAG. Detailed docs: `docs/src/explanation/observability.md`.
- **NetworkPolicies + Pod Security Admission**: every namespace this repo manages is default-deny on ingress and egress, with explicit allow rules for the traffic it actually needs, plus an explicit PSA level (`restricted` where a chart supports it, `baseline` where a one-shot Helm hook Job doesn't expose enough of a `securityContext` to reach `restricted`, `privileged` only for the 3 namespaces with a genuine `hostNetwork`/`hostPID`/`hostPath` need — `metallb-system`, `csi-driver-nfs`, `monitoring`). Lives as `kubernetes_network_policy`/namespace-label resources directly in whichever unit already owns that namespace, referencing other namespaces by their automatic `kubernetes.io/metadata.name` label rather than any new Terragrunt `dependency` wiring. Detailed docs, including the specific chart-default gaps found live: `docs/src/explanation/network-policies.md`.
- **Remote Tofu state**: a plain `local` backend per unit, all rooted under `tofu/state/` — a gitignored directory meant to be an SMB mount of a NAS share, set up out-of-band (not Tofu-managed) — chosen specifically so state storage never depends on infrastructure this config itself creates/destroys (no "destroying your own state backend" risk on a full teardown) and without introducing a new external dependency (cloud object storage). Each unit's exact path is generated by `tofu/live/root.hcl`'s `remote_state` block from directory structure, not hand-typed. See `docs/src/explanation/remote-state.md` for the SMB mount setup.
- **Dev/prod separation**: both `tofu/live/dev/` and `tofu/live/prod/` are live, each the identical seven-unit shape with real (not placeholder) `env.hcl` values — separate VLANs, separate NFS exports, separate Ceph pools, separate state trees, nothing shared except the physical Proxmox/Ceph hardware underneath. See `docs/src/explanation/terragrunt-units.md`.

## Toolchain

Every CLI below is version-pinned and installed via **[mise](https://mise.jdx.dev/)** (`mise.toml` at the repo root) — install mise itself first, then `mise trust && mise install` from the repo root gets a fresh machine every tool in one step, at the exact versions this repo is verified against. Bumping a pin is a deliberate edit to `mise.toml` followed by `mise install`, the same "variablize it, don't hardcode a moving target" philosophy this repo already applies to Helm chart versions in `env.hcl`. `mise activate` in your shell rc (or `mise exec -- <cmd>` per-command) puts the pinned versions on `PATH`; run `mise doctor` if a command resolves to a different version than expected.

- **OpenTofu** (not Terraform) — `tofu` CLI. **Terragrunt** orchestrates it (see Kubernetes cluster section above) — config lives in `tofu/modules/` (plain Tofu root modules, one per unit) and `tofu/live/` (per-environment Terragrunt wiring); commands are run as `terragrunt ...` from a `tofu/live/<env>/<unit>/` directory (or `tofu/live/<env>/` for `run --all`), not `tofu ...` directly. Detailed docs: `docs/src/explanation/terragrunt-units.md`. State lives on an SMB-mounted NAS share — a fresh machine needs that share mounted at `tofu/state/` before running `terragrunt init`, or Tofu will happily create fresh empty local state files instead of finding the real ones.
- **sops** + **age** — secrets encryption, see below.
- **gh CLI** — authenticate as ZanyHunter over HTTPS after install (`gh auth login`; mise only manages the binary, not the auth session). Used for the branch/PR workflow (see Git workflow below).
- **mdBook** — pinned to v0.5+ (required for the `mdbook-tabs` preprocessor below, which targets mdBook 0.5's preprocessor protocol and fails to parse against 0.4.x). Docs source is `docs/src/`; the built site `docs/book/` is gitignored (not committed) — run `mdbook build docs` locally to preview, no need to commit output. **mdbook-tabs** (mise's `cargo:` backend, which is also why `rust` is pinned in `mise.toml` — mise builds it from crates.io using its own managed Rust toolchain, no separate `cargo install` step) adds the global Production/Development domain-suffix tab switcher used throughout `docs/src/guides/` and `docs/src/explanation/` — see `docs/book.toml`'s `[preprocessor.tabs]`. Its `{{#tabs}}`/`{{#tab}}` markup only works as a standalone top-level Markdown block, not nested inside a list item or inline mid-paragraph (confirmed live — both produce broken/unparsed output), so a tabbed hostname mention is deliberately pulled onto its own line rather than left inline in surrounding prose.
- **kubectl** — pinned in `mise.toml` close to (not necessarily identical to) the cluster's own Kubernetes version, per `kubectl`'s ±1-minor version-skew policy — check the actual server version (`kubectl version`) before bumping this pin far ahead of it. This session's Bash environment has confirmed LAN reachability to `192.168.160.0/27` (verified via `ping` to a control-plane node and the gateway, 2026-08-27) — don't assume otherwise. If `ping`/`kubectl` fail, check first rather than declaring an environment limitation: the cluster's CA/certs regenerate on every from-scratch rebuild, so a cached `~/.kube/config` from before a rebuild will fail TLS verification even though the network path is fine — fetch a current one with `terragrunt output -raw kube_config` (from `tofu/live/dev/talos-cluster/`) rather than assuming a stale kubeconfig is still valid. Since #44, `kubectl` is also a hard *automation* dependency, not just an interactive convenience: `core-addons`'s `null_resource.external_secrets_crds` shells out to it via a `local-exec` provisioner (building its own throwaway kubeconfig from Tofu-known connection details, not `~/.kube/config`) to apply ExternalSecrets Operator's CRDs outside Helm's own lifecycle — a `terragrunt apply` of that unit fails outright on a machine without `kubectl` on `PATH`.
- **velero CLI** — pinned in `mise.toml`, **must be kept in lockstep with `chart_versions.velero`** in `tofu/live/<env>/env.hcl`: that value pins the Helm chart, whose `Chart.yaml` `appVersion` is the matching CLI/server version (currently chart `12.1.0` → CLI/appVersion `1.18.1`, `mise.toml`'s current pin) — a mismatched CLI can still work day-to-day but isn't guaranteed compatible with the server. When bumping `chart_versions.velero`, bump `mise.toml`'s `velero` pin to match in the same change. See `docs/src/explanation/backup-and-restore.md`.

## Secrets management

Provider credentials (Proxmox, Unifi, Cloudflare) live encrypted at `tofu/secrets.enc.yaml`, committed to git. Encrypted with SOPS, recipient is a single age keypair.

- **Decryption**: Terraform reads the file automatically via the `carlpett/sops` provider at plan/apply time — no manual decrypt step needed for normal Tofu usage. Since the Terragrunt refactor, the actual `data "sops_file"` block is generated per-unit from `tofu/live/root.hcl`'s `secrets_tf` local, only into the units that need decrypted credentials (`network`, `talos-cluster`, `core-addons`, `observability`) — see `docs/src/explanation/terragrunt-units.md`.
- **Manual edit**: `sops tofu/secrets.enc.yaml` (opens decrypted in `$EDITOR`, re-encrypts on save).
- **The age private key** lives at `~/.config/sops/age/keys.txt` on this machine (mode 600) — that's the default lookup path for both `sops` and the Terraform provider. It is **not** in git and never should be.
- **Disaster recovery for the key itself**: the private key is backed up in the user's KeePass file, which is itself stored offsite separately from this repo. If this machine is lost, recover the key from KeePass and place it back at `~/.config/sops/age/keys.txt` before running `tofu` commands.
- **Design note**: this is a single age key covering all secrets (Proxmox, Unifi, Cloudflare) — an honor-system boundary, not a technical one. We considered splitting Unifi/Cloudflare into a separately-keyed file so Claude could be structurally prevented from touching them, and decided against it: the user has a fresh full Unifi backup, so the blast radius of an honor-system violation is a few minutes of restore time. Revisit this if that risk tolerance changes.
- **GitOps secrets (ksops)**: ArgoCD's repo-server also has this same age key, mounted as a `kubernetes_secret` in the `argocd` namespace (the `core-addons` unit, `tofu/modules/core-addons/main.tf`), so it can decrypt SOPS-encrypted manifests under `apps/` at sync time via [ksops](https://github.com/viaduct-ai/kustomize-sops). This means the raw private key now lives in one additional place beyond this machine + KeePass: as a live Kubernetes Secret in-cluster. Accepted for the same reason as the single-key design above (dev cluster, single operator); revisit alongside that note if the risk tolerance changes. See `docs/src/explanation/gitops-app-of-apps.md` for how to add a new encrypted secret this way.
- **Tofu-generated secrets consumed by GitOps apps (ExternalSecrets Operator, #42)**: ksops is for secrets with no other source of truth; it's the wrong tool for a value Tofu also independently generates (a Keycloak OIDC client's `client_secret`) and needs to keep matching, since a ksops-encrypted copy silently drifts the moment the Tofu side regenerates it (a `keycloak-realm` destroy/recreate, a deliberate rotation). [External Secrets Operator](https://external-secrets.io/) (`helm_release.external_secrets`, `core-addons`) closes this gap for exactly that subset — no Vault, no new secrets-store service, it just mirrors a Kubernetes Secret Tofu writes into a `keycloak-secrets` namespace (owned by `core-addons`, populated by `keycloak-realm` — split this way since #44, so the namespace reliably exists before ArgoCD's first sync of the RBAC targeting it) out into each real app's own namespace. Its own access surface is deliberately narrow: the RBAC backing its `ClusterSecretStore` (`apps/cluster-addons/base/`, GitOps-managed) grants read-only access to Secrets in the `keycloak-secrets` namespace and nowhere else — no access to `tofu/secrets.enc.yaml`, no access to any other namespace's Secrets. See `docs/src/explanation/gitops-app-of-apps.md`'s ExternalSecrets section.

## Standing permissions & guardrails

- **Proxmox and Kubernetes (dev cluster)**: full autonomy. Run `terragrunt`/`tofu plan`/`apply`/`destroy`, `kubectl`, `helm` freely against this stack without asking first — it's dev-only and disposable.
- **Unifi**: do **not** author or apply changes to Unifi-managed Tofu resources (currently `unifi_network.this`, in the `network` unit, and the `unifi` provider block) that change what's actually deployed — new Unifi resources, address space changes, or any other config change — without asking the user first. It does **not** mean avoiding the files entirely: read-only operations (`tofu plan`) and mechanical formatting (`tofu fmt` and similar tools) are fine even if they happen to touch a line inside a Unifi resource block, since those don't change intent or behavior. **Exception, explicitly authorized**: each cluster (dev today, a future prod) has exactly one Unifi VLAN tied to that cluster's own lifecycle — standing it up or tearing it down as part of a normal cluster destroy/recreate cycle is pre-approved and needs no per-instance sign-off or `-target` workaround, as long as its configuration doesn't change in the process.
- **Cloudflare/DNS**: same ask-first treatment as Unifi. The token controls a real domain; cert-manager's existing use of it (DNS-01 challenges) is already approved and needs no further confirmation, but any new Cloudflare-related Tofu resource or config change should be proposed and confirmed before applying.
- **Everything else** (docs, non-secret Tofu resources, CI config, etc.): normal judgment — no special standing restriction beyond the git workflow below.

## Git workflow

- **Commit messages**: Conventional Commits, always (`feat:`, `fix:`, `docs:`, `chore:`, etc., with a scope where it adds clarity, e.g. `feat(tofu): ...`). Split unrelated concerns into separate, self-consistent commits rather than one large commit — each commit should leave the repo in a valid state.
- **Branching**: do not push directly to `main`. Create a feature branch, commit there, push it, and open a PR with `gh pr create`. Wait for the user to review and merge — do not merge PRs unilaterally.
- **Branch naming**: `type/short-description` (e.g. `feat/argocd-app-of-apps`, `docs/dns-guide`), mirroring the commit type.

## Tasking workflow

How the user hands off work, especially for larger refactors/features tracked over multiple sessions:

- **GitHub Issues are the backlog.** One issue per larger refactor or feature, with a checklist of subtasks in the body. Claude Code does not retain memory across sessions by default, so an issue is durable state a future session can re-read cold — treat it as more reliable than chat history for "what are we doing and why."
- **No passive awareness.** Claude does not see new GitHub comments, issues, or PR activity as they happen — only when a session actively runs a `gh` command. Nothing happens automatically just because something was posted on GitHub.
- **To task Claude**: point it at the issue number in a live session (e.g. "pick up #4"). It should pull the issue via `gh issue view`, do the work with full local access (secrets, LAN, everything this environment has), open a PR per the Git workflow above, and comment back on the issue linking the PR (or close it once merged).
- **The `@claude` GitHub App/Action integration** (triggers an automated run when `@claude` is mentioned in an issue/PR, no local session needed) was considered and deliberately skipped for now: that integration runs on an isolated GitHub Actions runner with access only to the git repo and configured Actions secrets — no local machine access, meaning no age key and no LAN reachability. Since nearly everything in this repo eventually touches secrets or needs LAN access, it would only cover a narrow slice of work (pure code review/refactor suggestions). Revisit if the balance of work shifts toward tasks that don't need local access.
- **GitHub Projects** (kanban board over issues) is worth adding once more than 3-4 larger initiatives are in flight concurrently; not needed yet.

## Documentation

Keep `docs/src/` in sync with infrastructure changes as part of the same change/PR — when a Tofu change alters operational procedure or adds/changes a subsystem, update the relevant mdBook page(s) alongside it rather than letting docs drift.

**Structure**: the site follows the [Diátaxis](https://diataxis.fr/) framework, four root sections under `docs/src/`. **`guides/`** is task-oriented how-tos (standing up the environment from scratch, onboarding a new app, restoring a backup) — a new subsystem's own architecture/reasoning does **not** belong here; add a short step or a link out instead. **`explanation/`** is where a new subsystem's "why" writeup and any real gotchas found deploying it actually belong — one page per subsystem, unnumbered, ordered roughly infra → add-ons → GitOps/apps → operational concerns. **`reference/`** is dry factual lookups (directory layouts, exact settings, hostname tables) — extend an existing table (e.g. `reference/deployed-apps.md`) rather than writing prose. **`tutorials/`** is a deliberate stub (see its index page) — this is a single-operator homelab, not an onboarding funnel; leave it alone unless the user asks otherwise.

**Publishing** (`.github/workflows/docs.yml`): builds and deploys via a `gh-pages` branch (GitHub Pages source is "Deploy from a branch", not the Actions-artifact deployment type — needed so production and PR previews can coexist as separate subdirectories of the same branch without one wiping the other). A push to `main` deploys to the branch root (`peaceiris/actions-gh-pages`, `keep_files: true`); every PR gets its own live preview at `zanylights.com/homelab/pr-preview/pr-<N>/` (`rossjrw/pr-preview-action`), GitLab "View app"-style — the action posts/updates a sticky PR comment with the link and automatically removes the preview directory when the PR closes, no separate cleanup job needed.

## Known gaps / roadmap

Worth revisiting as this matures beyond "dev, half-baked":

- **No CI** — no automated `tofu plan`/`validate` on PRs yet. Worth adding once the branch/PR workflow is in regular use.
- **NAS is not IaC-managed** — manual today.
- **Single age key covers all secrets** — see Secrets management design note above.
- **Immich is the only public app today** — prod's Cloudflare Tunnel is live at `photos.thepugh.family` (plus Keycloak's path-scoped `/realms/homelab/*` route the login flow needs), after the legacy Immich instance was migrated off that hostname to `<service>-legacy.thepugh.family` to free it (see the History entry below). The other five real apps under `apps/` (Actual Budget, Paperless-ngx, Vaultwarden, Homebox, changedetection.io) still need their own onboarding pass through `docs/src/explanation/public-ingress.md`'s runbook — nothing about the mechanism blocks it, they just haven't been added to `public_apps` yet.

## History / key decisions

- Chose SOPS + age over a NAS-hosted secrets store (e.g. Vault/MinIO) specifically because the user wanted secrets to survive a NAS-down disaster; encrypted secrets committed to git satisfy that, since any clone of the repo has the ciphertext, and only the age key (KeePass-backed, offsite) is the recovery-critical piece.
- Migrated `tofu/secrets.auto.tfvars` (plaintext, gitignored, single point of failure) to `tofu/secrets.enc.yaml` + `carlpett/sops` provider — see commit `1625bc3`.
- Decided against the `@claude` GitHub App/Action integration for now, in favor of GitHub Issues as backlog + live-session tasking — see Tasking workflow above for the reasoning (local secrets/LAN access it can't replicate).
- Bootstrapped the ArgoCD app-of-apps ApplicationSet and wired ksops into the repo-server for GitOps-managed secrets, migrating the MetalLB pool and cert-manager `ClusterIssuer`s off Tofu `terraform_data`/`local-exec` in the process — see PR #22 (closed issues #6, #7). ksops/SOPS-in-ArgoCD was chosen over External Secrets Operator/Vault specifically to avoid running and securing a new component, extending the same age-key pattern already used for `tofu/secrets.enc.yaml`.
- Deployed Velero + MinIO for cluster backups (closed issue #8) — MinIO because Velero needs an S3 API and doesn't speak NFS natively, so it's the gateway in front of the NFS share rather than a separate secrets-style store (see the SOPS-vs-MinIO decision above, which is about a different concern). Verified with a real backup → delete-namespace → restore round-trip, not just a `Completed` status.
- Deployed Keycloak + Postgres for SSO (closed issue #9), with the realm/clients managed via the `keycloak` Terraform provider rather than a Job/script — extends the same "declarative provider talks to the app's own API" pattern already used for `unifi_network.this`. Postgres is a hand-rolled `StatefulSet` rather than a chart, for the same pinned-version reason MinIO avoided Bitnami (see the backup decision above). Verified with a real end-to-end login (not just "the client exists"): scripted the full OIDC authorization-code flow against the `demo` realm user through oauth2-proxy forward-auth, confirming `X-Auth-Request-User`/`-Email` reached the protected app.
- Moved Tofu state off local-only storage (closed issue #12). First attempt was a remote S3 backend on the existing MinIO instance (a new `tofu-state` bucket, exposed via a new Ingress since MinIO had no reachable endpoint outside the cluster) — worked, including a real verified test of OpenTofu's native S3 conditional-write locking (two concurrent `tofu plan` runs, the second rejected with a `412 PreconditionFailed`), but was reverted after recognizing a structural problem: MinIO is created *by* this Tofu config, so destroying the cluster would eventually destroy the very thing backing its own state, and the final state-persist write would fail as a result. Replaced with a plain `local` backend pointed at a gitignored directory meant to be an SMB mount of a NAS share, set up out-of-band — the NAS already backs every cluster's NFS storage and already has its own offsite backup, and unlike the MinIO approach, state storage now depends on nothing this config itself creates or destroys, for any cluster (dev, a future prod, or otherwise). Also surfaced the same structural point either approach shares: backend blocks can't reference variables, so the backend's location is a hardcoded literal — exactly what prompted filing issue #26 (Terragrunt) as future work.
- Adopted Terragrunt, splitting the monolithic root module into six independently-applied units (closed issues #21, #26) — see `docs/src/explanation/terragrunt-units.md`. Fixes the Keycloak two-phase bootstrap for good (splitting `keycloak-infra`/`keycloak-realm` means `dependency` blocks, not `-target`, enforce ordering) and gives real dev/prod separation (each environment is its own directory of the same six units, `prod` scaffolded with placeholder values but never applied). Existing live state was migrated resource-by-resource via `tofu state mv` into each unit's new state file, verified zero-diff before any old file was deleted. Verified live with a real full destroy-then-recreate of the entire dev environment (not just "the config validates") — caught and fixed three genuine bugs in the process: `helm_release.ingress_nginx` needed `wait = false` (Helm's default wait-for-LoadBalancer-IP behavior deadlocks on a from-scratch bootstrap, since MetalLB's IPAddressPool is GitOps-managed and can't exist until ArgoCD — which depends on ingress-nginx — has already synced); every `dependency` block's `mock_outputs_allowed_terraform_commands` needed `"destroy"` added (without it, `run --all destroy` couldn't clean up a stack a failed apply had left partially applied, since a downstream unit hard-fails resolving a dependency with no real outputs yet — safe to allow, since a unit with nothing to destroy never touches its provider regardless, and `"apply"` stays deliberately excluded so the Keycloak fix above is unaffected); and oauth2-proxy's Ingress needed `nginx.ingress.kubernetes.io/proxy-buffer-size: "16k"` (its session cookie bundling Keycloak's access/ID/refresh tokens routinely exceeded ingress-nginx's default buffer, 502ing every real login — a pre-existing bug on the original flat module too, just never hit before; re-verified with the same real scripted-login bar used for #9). Repeated back-to-back destroy/recreate cycles against the same 3 physical Proxmox nodes (done deliberately, beyond what was asked, to pressure-test the one-command claim under adverse conditions) also surfaced real but environmental flakiness — a transient DNS failure resolving `factory.talos.dev`, and Talos VMs that repeatedly failed to become network-reachable for several cycles in a row even though Proxmox reported them created — both resolved by simply retrying once the physical hardware had a chance to settle, not a defect in the refactor.
- Deployed an observability stack — kube-prometheus-stack (Prometheus/Alertmanager/Grafana/node-exporter/kube-state-metrics) + Loki + Grafana Alloy for logs (closed issue #14), a seventh Terragrunt unit sibling to `backup`/`keycloak-infra`. Alertmanager routes to a Discord webhook; cert-manager cert-expiry and Velero backup-staleness alerts are added via the chart's own `additionalPrometheusRulesMap`/`additionalScrapeConfigs` values rather than separate `ServiceMonitor`/`PrometheusRule` custom resources in `core-addons`/`backup`, specifically to avoid a real CRD-before-CR ordering conflict those units' earlier-in-the-DAG position would create. Verified live, not with a mocked-up "the pods are Running" check: a synthetic alert POSTed directly to Alertmanager was confirmed to actually arrive in Discord by the user watching the channel. Two real Talos incompatibilities were found and fixed in the process: kube-prometheus-stack's default `kube-proxy`/`kube-scheduler`/`kube-controller-manager` scrape jobs assume kubeadm-style host-network exposure that Talos doesn't provide (bound to loopback only), producing permanent `TargetDown` alerts until those three jobs and their default alert-rule groups were disabled; and Loki's `chunksCache` (an optional memcached query-cache, on by default) requests ~8Gi of memory — more than any single node in this 4GB-RAM-per-node cluster has — leaving it permanently unschedulable until disabled, the same class of oversized-chart-default problem already hit once with MinIO. See `docs/src/explanation/observability.md`.
- Rolled out default-deny NetworkPolicies plus an audited/tightened Pod Security Admission level across every namespace this repo manages (closed issue #31, split from #13 at the user's request into three focused issues — see #32, #33 for the other two). Applied and verified one namespace group at a time rather than all at once, given the real risk of breaking already-verified functionality — every previously-established verification bar in this repo (a real Velero backup, a real Keycloak/oauth2-proxy login, Grafana/Prometheus/Loki/Discord alerting, ArgoCD sync, a real PVC provisioning) was re-run and passed after its namespace group's rollout, not assumed to still work. `metallb-system`/`csi-driver-nfs`/`monitoring` stay at the already-justified `privileged` level (genuine `hostNetwork`/`hostPID`/`hostPath` needs); `argocd`/`cert-manager`/`ingress-nginx`/`keycloak`/`sso-demo` reached `restricted`; `minio`/`velero` landed on `baseline` after a one-shot Helm hook Job in each (not the main workload) turned out not to expose enough of a `securityContext` value to go further. Found and fixed several real gotchas along the way: an `allow-same-namespace` policy is now unconditional for every namespace rather than judged pod-by-pod, after a default-deny rollout without one left MinIO's own post-install bucket-creation Job hanging with no clear error; Postgres's *existing* data directory turned out to be owned by uid 70, not the uid 999 guessed at first, caught via a throwaway debug pod against the same PVC before it corrupted anything; and a StatefulSet's controller left a crash-looping pod running under its *old* spec for several minutes after a fix was applied, needing a manual pod delete to force it to pick up the correction. See `docs/src/explanation/network-policies.md`.
- Wired ArgoCD and Grafana into Keycloak SSO via their own native OIDC support, and disabled local admin on both once real Keycloak login was verified live (closed issue #32, split from #13). Access is deliberately group-based rather than identity-based, at the user's explicit request to keep personal identity out of this repo's code: a `platform-admins` Keycloak group (`keycloak-realm`'s `keycloak_group.platform_admins`) plus a reusable `groups` client scope/protocol mapper is the only thing either app's RBAC checks (ArgoCD's `policy.csv`, Grafana's `role_attribute_path`) — no `keycloak_user` resource for a real person exists, and real accounts/group membership are managed by hand in Keycloak's admin console. Each client's secret is generated in the unit that actually consumes it (`core-addons` for ArgoCD, `observability` for Grafana) and passed downstream to `keycloak-realm` to create the matching client — the same "secret generated upstream, consumed downstream" shape already proven for `keycloak-infra`'s admin password, needed because `core-addons`/`observability` apply before `keycloak-realm` in the DAG and a direct dependency the other way around would cycle back through `keycloak-infra`. This is what makes `observability` a new upstream dependency of `keycloak-realm` (see the Observability bullet above). Upstream identity federation (e.g. Google via Keycloak) was considered and explicitly deferred — local Keycloak accounts are sufficient for a single-operator homelab today; see `docs/src/explanation/sso-and-keycloak.md`. Found and fixed one real gotcha live: the ArgoCD chart's default `configs.cm.url` (`https://argocd.example.com`) is what ArgoCD actually uses to build its OIDC `redirect_uri`, not the incoming request's `Host` header — left unset, Keycloak correctly rejected every real login attempt as an invalid redirect URL.
- Retired the `k8s.thepugh.family` subdomain scheme entirely in favor of one per-environment `domain_name` value driving every ingress hostname/OIDC redirect URI across `core-addons`/`keycloak-infra`/`keycloak-realm`/`observability` (closed issue #10): dev moved to `dev.thepugh.family`, prod's placeholder to the real `thepugh.family` apex. DNS resolution for the whole suffix became a single Tofu-managed wildcard record (`unifi_dns_record.wildcard_ingress`, the `network` unit) instead of a manually-created one — deliberately a wildcard rather than per-hostname records (ingress-nginx already centralizes every hostname behind one LoadBalancer IP, so one record gives every current and future `apps/<app>/` addition zero-touch DNS, the same promise app-of-apps already makes for everything else) and deliberately no new component like ExternalDNS (the wildcard already gives that same zero-touch benefit for free). The `network` unit's new DNS record needed a static `ingress_ip` `env.hcl` value rather than reading ingress-nginx's live MetalLB-assigned IP as a Tofu output, specifically to avoid a DAG cycle (`network` applies before `core-addons`, which owns ingress-nginx) — `core-addons` now pins ingress-nginx's Service to that same static IP via the `metallb.universe.tf/loadBalancerIPs` annotation instead of letting MetalLB assign it dynamically. The old manually-created `*.k8s.thepugh.family` Unifi DNS record was imported into Tofu state and destroyed cleanly through a real `plan`/`apply` (proving the import first via a zero-diff `plan`) rather than left as an unmanaged leftover or deleted by hand outside Tofu's audit trail — explicitly user-authorized for this one record, distinct from every other pre-existing Unifi DNS record, which stayed untouched. Verified live end-to-end: the new wildcard resolving correctly, the pre-existing external `homeassistant.thepugh.family` record confirmed unaffected throughout, cert-manager re-issuing real certs for every moved hostname (DNS-01 needed no config change — it doesn't care about subdomain depth within the zone), a full scripted OIDC login re-run against the new `sso-demo.dev.thepugh.family`, and the user re-confirming real interactive ArgoCD/Grafana logins on their new hostnames.
- Added a Ceph-backed StorageClass (`ceph-rbd-<cluster_name>`) alongside the existing NFS one, for databases and other block-semantics-sensitive workloads where NFS's fsync/locking semantics are a real correctness risk (closed issue #28) — general file storage stays on NFS. ceph-csi (RBD, external-cluster mode) connects directly to the existing Proxmox Ceph cluster; no Ceph daemons run inside Kubernetes, no new hardware — a dedicated pool (`proxmox_ceph_pool`, newly available in the `bpg/proxmox` provider, which needed bumping from `~> 0.82.1` to `~> 0.111.0`) within the same physical cluster already backing Talos VM disks, kept separate specifically so Kubernetes' Ceph credential can't reach VM disk storage. The CephX credential itself can't be Tofu-managed (Ceph's own auth system, distinct from Proxmox's PVE user management, which the provider does manage) — bootstrapped manually and stored in `tofu/secrets.enc.yaml` like every other provider credential; documented as a required manual step for any future from-scratch stand-up, `docs/src/explanation/ceph-backed-storage.md`. Verified with a real workload, not just a bound test PVC: migrated Keycloak's existing Postgres onto it (`pg_dump`/restore, a fresh Velero backup taken first as an extra safety net), confirmed with a real scripted OIDC login against the migrated data afterward. Found two real gotchas live: changing a StatefulSet's `volumeClaimTemplate` and letting Tofu replace the StatefulSet does *not* create a new PVC when one with the expected name already exists — Kubernetes silently reuses the old one regardless of the new StorageClass, so the old PVC has to be explicitly deleted (and the StatefulSet's declared `replicas` can silently re-create a pod that re-binds to it first, if you're not watching for that); and a freshly-formatted Ceph RBD volume is root-owned by default, so a non-root workload (Postgres, uid 70) needs an explicit `fs_group` it never needed on NFS (root-squash was disabled on that export instead).
- Closed the backup gap the Ceph-backed StorageClass work above surfaced but didn't fix: enabled Velero's File System Backup (`deployNodeAgent = true`, its kopia integration) for real volume *data*, not just object definitions — CSI volume snapshots stay disabled, since File System Backup already covers both the NFS- and Ceph-backed StorageClasses with one mechanism rather than needing a second, Ceph-only one. Opt-in per-pod via the `backup.velero.io/backup-volumes` annotation (applied to Keycloak's Postgres first) rather than backing up every volume in the cluster by default, at the user's explicit preference for a consistent (same tool, not a new one) but deliberately conservative solution. Required moving the `velero` namespace to `privileged` PSA (node-agent's DaemonSet needs real host-level volume-path access, same category of need as the cluster's other node-level DaemonSets) and a real gotcha found live on the first backup attempt: kopia's repo path resolves off `$HOME`, unset under the main Velero container's non-root `runAsUser` with no matching `/etc/passwd` entry, producing a real `mkdir /udmrepo: permission denied` — fixed with an explicit `HOME=/tmp` env var. Verified with the same real backup → delete-namespace → restore round-trip bar already established for Velero, re-run specifically against a Ceph-backed PVC with real file data (not just a `Completed` status) rather than against the live Keycloak namespace, to prove the mechanism without risking real SSO data.
- Resized worker nodes from 4 vCPU/4GB to 8 vCPU/16GB (control planes stay at 4 vCPU/4GB — nothing schedules there, already proven adequate), and turned the previously-hardcoded, non-role-differentiated `cpu`/`memory` VM settings into a `node_resources` object variable (`env.hcl`-driven, same `chart_versions`/`nfs_storage` pattern). Prompted by the Immich work below needing real headroom for its ML service, on top of an existing known gap: Loki's `chunksCache` had been disabled outright rather than tuned down, on the mistaken assumption its ~8Gi default was a fixed requirement rather than an independently tunable `allocatedMemory` value — re-enabled both `chunksCache`/`resultsCache` at 512MB now that there's room, correcting that account in `docs/src/explanation/observability.md`. Verified live with a real one-worker-at-a-time cordon/drain/reboot cycle (never touching more than one worker, and never touching control planes, so etcd quorum was never at risk) — zero pods left unhealthy, all three workers confirmed at the new size afterward.
- Stood up a Tofu-managed Cloudflare Tunnel for public ingress (closed issue #33, first real implementation of the design doc from PR #39) — a new `cloudflare` Terraform provider (first use in this repo), `cloudflare_zero_trust_tunnel_cloudflared`/`_config` for the Tunnel object and its allowlist-only ingress rules, `cloudflare_dns_record` for the public CNAMEs, and a hand-rolled `cloudflared` Deployment (2 replicas, native multi-connector redundancy, `--protocol http2` to keep the NetworkPolicy TCP-only). The ingress config is the actual mechanism keeping Keycloak's admin console internal/VPN-only: every `env.hcl` `public_apps` entry forwards its full hostname, Keycloak gets exactly one path-scoped `^/realms/homelab/.*` route, and a `http_status:404` catch-all sits last — nothing else is ever forwarded, not a denylist rule that could miss a path. The one manual step (widening the existing Cloudflare API token's scope to add Account: Cloudflare Tunnel Edit, done with the user's explicit sign-off per the standing Cloudflare guardrail) mirrors the CephX credential handoff precedent — the token's *value* never changed. Verified live: `cloudflared` registered healthy connections to Cloudflare's edge across 4 locations, and the loaded config matched the intended rules exactly. A real gotcha found live once the user actually tested the public hostname from outside: Cloudflare's free Universal SSL only covers one subdomain level (`*.thepugh.family`), not `domain_name`'s own depth (`photos.dev.thepugh.family` is two levels down) — a real `ERR_SSL_VERSION_OR_CYPHER_MISMATCH`, not a tunnel/ingress misconfiguration. Fixed for free (rather than paying ~$10/month/zone for Advanced Certificate Manager) by putting public hostnames one level under the real apex instead (`public_apex_domain` + `public_hostname_suffix`, e.g. `photos-dev.thepugh.family`), with `cloudflared` rewriting the Host header back to the real internal hostname when forwarding to ingress-nginx — the user's own suggestion of an env-suffixed public hostname, which also fixed a latent bug in the first draft (a bare `photos.thepugh.family` for dev would have collided with prod's eventual public hostname for the same app). Full outside-the-LAN verification of the public path is a check this repo's own tooling still can't perform (this session's environment resolves `*.dev.thepugh.family` to the internal LAN answer regardless of which DNS resolver is queried), left for the user to confirm from a real external network.
- Deployed Immich (closed issue #33's remaining scope) — the first real workload under `apps/`, not a cluster add-on, reachable at `photos.dev.thepugh.family` internally (briefly also public via the new Cloudflare Tunnel — see the entries above/below for why that's since been reverted). Database on the Ceph-backed StorageClass using `tensorchord/vchord-postgres` (a purpose-built VectorChord image, not CloudNativePG — immich-charts' own recommended path, but a whole new operator/CRDs this repo's minimal-new-components philosophy pushed against for one database); media library on the NFS-backed one. Auth via Keycloak's native OIDC support (`keycloak_openid_client.immich`, `keycloak-realm`), verified with a real OAuth authorize-URL round-trip against the live realm, not just "the client exists." The chart itself is a pinned `helm template` render committed as static YAML rather than rendered live via Kustomize's `helmCharts:` — that combination has a real, currently-unresolved OCI-pulling bug upstream (kubernetes-sigs/kustomize#4381, argoproj/argo-cd#21257), discovered before committing to the approach rather than after. Found and fixed one real gotcha live: neither the `immich-server` nor `immich-machine-learning` images ship a built-in non-root user, so `restricted` PSA rejected both on the first deploy attempt (`container has runAsNonRoot and image will run as root`) — fixed with an arbitrary `runAsUser`/`fsGroup` (root-squash already disabled on the NFS export, so the exact UID doesn't matter). Verified with a full live deployment via direct `kubectl apply` before ever pushing to GitOps — every PVC bound, every pod healthy, a real end-to-end OAuth authorize URL confirmed correct — with zero diff between the committed manifests and the verified-working live state by the time it was handed to ArgoCD. See `docs/src/explanation/immich.md`.
- Tore dev's public ingress back down and re-scoped the long-term plan to prod-only, after the very first real public login attempt surfaced a second gotcha beyond the Universal SSL one above: Keycloak rejected Immich's login with `Invalid parameter: redirect_uri`, because Immich computes its `redirect_uri` from whichever hostname the browser actually used (`photos-dev.thepugh.family` publicly) but only the internal hostname's callback URLs were ever registered on `keycloak_openid_client.immich`. Rather than adding the missing redirect URI, the user set the actual long-term direction this was all headed toward anyway: only prod is meant to be publicly exposed — dev's exposure was explicitly temporary, proving the mechanism before an existing Immich instance elsewhere (`photos.thepugh.family`, unrelated to this repo) gets migrated to a legacy domain ahead of prod's eventual stand-up. Since prod's own `domain_name` is already the bare apex, it was never going to hit either gotcha (no hostname split, so no split redirect URIs either) — closing the loop cleanly rather than patching around a problem prod won't actually have. Added `public_ingress_enabled` (`env.hcl`; `false` for dev, `true` for prod) gating every Cloudflare Tunnel resource via `count`, not just the routing — disabling it removes the Tunnel object, `cloudflared`, and the DNS records entirely, leaving zero public-ingress footprint in dev rather than idle infrastructure. The `core-addons` module code itself needed no environment-specific rewrite, only new `env.hcl` values — the same dev/prod-share-code-differ-in-values pattern this repo uses everywhere. Verified live: all 13 Cloudflare Tunnel resources destroyed cleanly, `cloudflared` namespace gone, Immich's internal LAN access confirmed completely unaffected, zero drift across all 7 units afterward.
- Deployed five more real apps under `apps/`, all following Immich's pattern (LAN/VPN-only, storage split by locking-sensitivity, hand-rolled Kustomize manifests, no live-rendered Helm charts): Actual Budget, Paperless-ngx, Vaultwarden, Homebox, and changedetection.io. Each got its own `keycloak_openid_client` in `keycloak-realm` (Tofu-generated secret, hand-carried into that app's ksops-encrypted config, same shape as Immich's). Four have native OIDC (`ACTUAL_OPENID_*`, django-allauth's `openid_connect` provider, Vaultwarden's `SSO_*` env vars, and Homebox's `HBOX_OIDC_*`); Paperless-ngx additionally syncs `platform-admins` to Django superuser via its own `PAPERLESS_SOCIAL_ACCOUNT_SYNC_*` group-claim support, reusing the same `groups` client scope already proven for ArgoCD/Grafana. changedetection.io has no OIDC/SSO integration at all (confirmed against its own tracker — a feature request open since 2022, unimplemented), making it the first *real* app to use the `oauth2-proxy` forward-auth template the `sso-demo` reference deployment exists to prove out, copied as plain manifests rather than `sso-demo`'s Tofu-managed Helm release. A real gotcha found live on the very first deploy: Actual Budget's server crashed with `port: ports must be within range 0 - 65535` — Kubernetes auto-injects Docker-links-style `<SVCNAME>_PORT` env vars into every pod for every Service in a namespace, and the `actual` Service's auto-injected `ACTUAL_PORT=tcp://<ip>:5006` (a URI string) collided with Actual's own `ACTUAL_PORT` config key, which expected a bare integer. Paperless-ngx has the identical exposure (its own listen-port setting is also literally named `PAPERLESS_PORT`, on a Service also named `paperless`) even though it hadn't yet broken — rather than patch only the app that failed, `enableServiceLinks: false` was set on every pod spec across all five apps, since none of them rely on legacy Docker-links-style service discovery. Verified live end-to-end for every app via a real OAuth authorize-URL round-trip against the live Keycloak realm (or, for changedetection.io, against oauth2-proxy's own redirect) with `client_id`/`redirect_uri` confirmed matching each Keycloak client exactly — not just "the pod is Running" — plus every PVC confirmed bound on its intended StorageClass and zero drift across all 7 Terragrunt units afterward. See `docs/src/explanation/self-hosted-apps.md`.
- Restructured every directory under `apps/` (`cluster-addons` plus all six real workloads) from a flat `apps/<app>/*.yaml` layout into `apps/<app>/base/` + `apps/<app>/overlays/{dev,prod}/`, after noticing (prompted directly by the user, while deploying the five apps above) that every one of them hardcoded `dev.thepugh.family` and `ceph-rbd-dev`/`nfs-dev` directly into committed YAML — meaning none of it would actually work once prod stands up, despite `env.hcl` already solving the identical problem on the Tofu side. `base/` stays environment-agnostic (placeholder tokens like `__APP_HOSTNAME__`/`__STORAGE_CLASS_CEPH__` wherever a real value would otherwise go); each `overlays/<env>/` is just a small `ConfigMap` of real values plus a `kustomization.yaml` `replacements:` block — deliberately `replacements:` over raw `patches:`, so each real value is written down exactly once per environment rather than repeated in a separate patch per field. The trick that kept this genuinely minimal: base files keep their real static suffixes (`/realms/homelab`, `/oauth2/callback`) intact, with only the hostname *segment* as a token, and `replacements:`' `options.delimiter/index` (splitting on `/`, touching one indexed segment) swaps just that token — reaching even a Keycloak issuer URL nested inside a ksops-encrypted secret's JSON blob (Paperless) or YAML blob (Immich), since `replacements:` operates on the fully-built resource list after ksops has already decrypted it, regardless of whether a field originated from a plain file or a generator. The ApplicationSet's git generator (`core-addons`) moved from `apps/*` to `apps/*/overlays/${var.cluster_name}` (reusing the existing `cluster_name` variable already used for `nfs-<cluster_name>`/`ceph-rbd-<cluster_name>` naming, rather than adding a redundant new one), with `{{path[1]}}` replacing `{{path.basename}}` for the generated `Application`'s name/namespace (which would otherwise resolve to `dev`/`prod`, not the app name) — each environment's own ArgoCD only ever discovers its own overlay, never the other's, even though both point at the same git path. Verified via the real `kustomize`+`ksops` binaries inside ArgoCD's own repo-server pod (this repo's local Bash environment has neither installed) for all 7 restructured directories, byte-for-byte diffed against live cluster state — not just "the build doesn't error." The Tofu side (`core-addons`'s `argocd_apps` Helm release) plans as a single isolated 1-resource change; deliberately not applied yet, since `var.gitops.revision` tracks `main` and applying before this PR merges would have ArgoCD's generator look for a directory structure (`apps/*/overlays/dev`) that doesn't exist in `main` until merge — safe either way thanks to `preserveResourcesOnDeletion`, but applying only after merge avoids a needless "Applications missing from the ArgoCD dashboard" window. Adding a new environment now means adding an `overlays/<new-env>/` to all seven directories, not touching one file — an explicit new checklist item in `docs/src/explanation/gitops-app-of-apps.md` rather than left as tribal knowledge.
- Deployed [External Secrets Operator](https://external-secrets.io/) to close issue #42 — a deliberate, narrow revisit of the ksops-over-ESO decision recorded above (#7): that reasoning still holds for every secret with no other source of truth, it just doesn't hold for the six real apps' Keycloak OIDC client secrets, which are also independently generated by Tofu and were drifting silently out of the ksops-encrypted copies on any `keycloak-realm` destroy/recreate or deliberate rotation. No Vault, no new secrets-store service — ESO's `kubernetes` provider just mirrors a Kubernetes Secret that `keycloak-realm` already writes into a new `keycloak-secrets` namespace it owns, out into each real app's own GitOps-managed namespace via a `ClusterSecretStore` (RBAC-scoped to read-only on that one namespace, nothing else) and one `ExternalSecret` per app. Immich's and Paperless-ngx's client secrets are each nested inside a larger config document (a YAML blob, a JSON blob) rather than a standalone Secret key — handled via `spec.target.template`, rendering the whole document as a Go template with just the live secret field interpolated, which also fixed a pre-existing over-encryption on Immich's side (the old ksops file encrypted its entire config document even though only one field was ever sensitive). Vaultwarden/Homebox/changedetection.io each co-locate a Tofu-sourced key with an unrelated one (an admin token, a pepper, a cookie secret) — split into two Secret objects rather than stretching one mechanism to own both, keeping the original name on whichever side kept more keys to minimize Deployment edits. Verified live end-to-end before ever pushing to GitOps (same "verify via direct kubectl apply, then match byte-for-byte once ArgoCD takes over" precedent as Immich's original deployment): a throwaway `ExternalSecret` in an isolated namespace confirmed the RBAC/`ClusterSecretStore` plumbing actually works, with the synced value byte-matched against `terragrunt output -raw actual_oidc_client_secret`. Also applied the `core-addons` ApplicationSet generator-path change that PR #41 had deliberately left unapplied pending merge — confirmed live afterward with all 7 `Application`s `Synced`/`Healthy` under the new `apps/*/overlays/<cluster_name>` path. See `docs/src/explanation/gitops-app-of-apps.md`'s ExternalSecrets section.
- Fixed three genuine ordering/lifecycle bugs (issue #44) surfaced by a real, fully unattended full-environment `terragrunt run --all destroy` + `apply` of dev — at the user's explicit direction, after an unrelated SSO-repair task turned out to be exactly the "opportunity to tear everything down and test a ground-up deployment" the "one-command stand-up/teardown" claim had never actually been exercised against end-to-end (the original Terragrunt-refactor proof, #21/#26, and the #42 ExternalSecrets verification, both predate this and neither happened to hit these races). `cluster-addons` and `keycloak-secrets` are now genuinely Tofu-managed namespaces (`kubernetes_namespace.cluster_addons`/`kubernetes_namespace.keycloak_secrets`, `core-addons`) rather than left for ArgoCD's `CreateNamespace=true` to create — true on every previously-live cluster (ArgoCD had long since synced by the time anyone looked), false on a from-scratch apply, where `core-addons`'s own apply can finish installing ArgoCD before ArgoCD has synced anything at all, failing the NetworkPolicies/RBAC that reference those namespaces. `keycloak-secrets` specifically moved out of `keycloak-realm` (the *last* of `core-addons`' four dependents to apply, often minutes after ArgoCD is already syncing `apps/cluster-addons/`'s RBAC against that same name) into `core-addons` itself, applied early alongside ArgoCD — `keycloak-realm` still populates the real Secret values, now via a new `core_addons` output rather than owning the namespace resource, migrated with a real `terraform state mv` between the two units' state files (not a destroy/recreate, which would have cascade-deleted all 6 apps' OIDC secrets exactly like the incident this whole exercise started from). The ArgoCD `ApplicationSet` template's per-`Application` `retry` policy was also raised well past ArgoCD's default (5 attempts, short backoff) as defense in depth. Separately, `external-secrets`' CRDs (bundled as regular chart templates, not Helm's protected `crds/` folder) were found to deadlock a real `helm uninstall` — deleting the `ExternalSecret` CRD blocks until every CR instance is gone, but each of the 6 GitOps-managed instances carries ESO's own cleanup finalizer, removable only by the controller being uninstalled in that same operation — fixed by setting `installCRDs = false` and applying the CRDs once via a plain `kubectl apply --server-side` a create-only `null_resource` local-exec provisioner runs (no destroy-time provisioner, so a `terraform destroy` never touches the live CRDs at all; the provisioner builds its own throwaway kubeconfig from `var.kubernetes_client_configuration` rather than relying on `~/.kube/config`, since a provisioner's shell can't reach the Tofu `kubernetes`/`helm` providers' own authenticated session). Rolling this specific fix out against dev's already-live cluster (as opposed to a future fresh bootstrap, which never hits this) hit a real, near-miss variant of the same deadlock class: flipping `installCRDs` from its implicit `true` default to `false` on an *existing* Helm release made that very upgrade prune the CRDs Helm had been tracking, briefly cascading away all 6 `ExternalSecret`/`ClusterSecretStore` instances and their generated Secrets — recovered live with no data loss (the canonical Secret values in `keycloak-secrets` were never touched) via a direct `kubectl apply` of the CRDs, a forced ArgoCD resync, and an ESO force-resync per app, verified byte-for-byte against `terragrunt output -raw actual_oidc_client_secret` afterward. `terragrunt run --all plan` confirmed zero drift across all 7 units once everything landed. See `docs/src/explanation/gitops-app-of-apps.md`'s "Bootstrap ordering gotchas" section.
- Adopted [mise](https://mise.jdx.dev/) as the single tool version manager for every CLI this repo's development and deployment work needs (OpenTofu, Terragrunt, sops, age, `gh`, `kubectl`, mdBook, `mdbook-tabs`, `velero`) — `mise.toml` at the repo root, versions live-tested via a real `mise install` rather than guessed from memory before being committed. Chosen over documenting a per-tool manual-install list (the prior state) because it turns "read a paragraph, run the right install command for your OS, hope the version matches what this repo was verified against" into `mise trust && mise install`, and because it closes a real, if minor, gap: `gh` and `age` on this machine had drifted years out of date (an apt-packaged `gh` from 2022, an `age` predating 1.1) without anyone noticing, since nothing had ever asserted what version *should* be installed. `mdbook-tabs` installs via mise's `cargo:` backend (building from crates.io with a mise-managed Rust toolchain, also pinned) rather than a curated registry entry, since it's too small/niche a crate to have one. The CI docs workflow (`.github/workflows/docs.yml`) was migrated from two hand-rolled `curl`+`tar` steps with their own separate `MDBOOK_VERSION`/`MDBOOK_TABS_VERSION` env vars to `jdx/mise-action`, scoped to just those two tools (`install_args`) rather than the whole toolchain, so CI and local dev now read the exact same version pins from one file instead of two independently-maintained ones. `velero`'s pin is the one that needs active upkeep: it must move in lockstep with `chart_versions.velero` in `env.hcl` (documented in both places) since a mismatched CLI isn't guaranteed compatible with the deployed server, unlike every other pin here which is free-standing.
- Stood up prod for real (closes the "prod is scaffolding" gap noted throughout this file) by first fixing a piece of standing debt: dev's Unifi network object had been named `"K8s-Cluster-Prod"` since before dev/prod were split apart, and was never corrected out of caution around the Unifi guardrail. Prod now genuinely takes over that name by taking over the VLAN it names — VLAN 1601 / `192.168.160.0/27`, the exact range dev used to occupy — while dev moved to a new, same-sized block (VLAN 1602 / `192.168.160.32/27`) and had `-dev-` added to its node hostnames (`k8s-dev-ctrl-00`, etc., vs. prod's clean `k8s-ctrl-00`) so the two never collide. Both environments' addressing is now laid out consecutively (gateway, then VIP, then nodes, then `ingress_ip`) rather than scattered. This is the first time dev and prod have ever coexisted against the same 3 physical Proxmox nodes, which surfaced several real bugs nothing had ever hit before (see `docs/src/explanation/terragrunt-units.md`'s and `ceph-backed-storage.md`'s gotcha sections for the full detail): `network_cidr` was hardcoded into two `core-addons`/`observability` NetworkPolicies instead of variablized; prod's `talos-cluster` `.terraform.lock.hcl` was still pinned to the pre-#28 `bpg/proxmox` version, never caught since prod had never been applied; the Unifi switch NIC-bounce delay (`time_sleep`, originally alongside `unifi_network.this` in the old flat module) was lost when `network`/`talos-cluster` split into separate Terragrunt units, and is now re-added inside `talos-cluster` itself; the Talos ISO's filename was derived from its (identical, same Talos version/schematic) download URL, so prod's first apply collided with a file dev's own state already owned; and `ceph_rbd_client_key` in `secrets.enc.yaml` was a single flat value rather than keyed per environment, silently correct only for whichever environment's key was currently stored. Also enabled prod's public ingress for the first time: Immich at `photos.thepugh.family` and Keycloak's path-scoped `/realms/homelab/*` route, after confirming live that the user's legacy Immich (and five other legacy apps) had already been moved to `<service>-legacy.thepugh.family`, genuinely freeing the hostname rather than assuming it. Each bug above was found and fixed live, mid-deployment, rather than caught ahead of time — including two more transient ones only the destroy path hit: `keycloak-realm`'s destroy issuing a DELETE against the live Keycloak API right as something else in the same run made it briefly unreachable, and a leftover cert-manager ACME `Challenge` in the `argocd` namespace whose `finalizer.acme.cert-manager.io` could never clear once cert-manager's own controller had already been torn down earlier in that same `core-addons` destroy, wedging the namespace in `Terminating` — resolved by stripping the stale finalizer by hand. A final `terragrunt run --all destroy` + `apply` of both environments from an absolute-zero state (VLANs included), with every code fix already in place, confirmed none of the *code* bugs recur: both environments came up in one apply attempt each, every PVC bound immediately (no `ceph-csi` provisioner restart needed this time), zero manual `kubectl` intervention, all 6 nodes `Ready` and all 7 `Application`s `Synced`/`Healthy` in both clusters, zero `terragrunt plan` drift across all 14 units. True reachability from outside the LAN remains the one check this repo's own tooling can't perform regardless and needs the user's own confirmation, per the same limitation noted in `docs/src/explanation/public-ingress.md`.
