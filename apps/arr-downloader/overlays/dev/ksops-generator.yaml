apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: arr-downloader-vpn-secret
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - ./vpn-credentials.enc.yaml
