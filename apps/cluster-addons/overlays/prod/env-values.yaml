# The per-environment values this directory's base/ manifests reference as
# __LB_POOL_RANGE_1__/__LB_POOL_RANGE_2__ tokens — see
# docs/src/explanation/gitops-app-of-apps.md for the base/overlays pattern.
# Placeholder shape mirroring dev's exact relative layout within prod's own
# subnet (192.168.161.0/27) — not yet a vetted real allocation; see
# tofu/live/prod/env.hcl's own placeholder framing.
apiVersion: v1
kind: ConfigMap
metadata:
  name: env-values
data:
  lb-pool-range-1: 192.168.161.5-192.168.161.8
  lb-pool-range-2: 192.168.161.12-192.168.161.14
