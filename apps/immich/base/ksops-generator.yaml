apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: immich-secrets
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - ./immich-config.enc.yaml
  - ./immich-db-credentials.enc.yaml
