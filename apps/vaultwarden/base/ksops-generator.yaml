apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: vaultwarden-secrets
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - ./vaultwarden-config.enc.yaml
